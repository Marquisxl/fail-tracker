<h1>失敗記録一覧</h1>

<div class="mb-3">
  <%= link_to "新しい失敗を記録", new_failure_path, class: "btn btn-primary" %>
</div>

<% if @failures.any? %>
  <div class="row">
    <% @failures.each do |failure| %>
      <div class="col-md-4 mb-4">
        <div class="card">
          <div class="card-body">
            <h5 class="card-title"><%= failure.title %></h5>
            <h6 class="card-subtitle mb-2 text-muted"><%= failure.date.strftime('%Y年%m月%d日') %></h6>
            <p class="card-text"><%= truncate(failure.description, length: 100) %></p>
            <span class="badge bg-info"><%= failure.category.name %></span>
            <div class="mt-2">
              <%= link_to "詳細", failure_path(failure), class: "card-link" %>
              <%= link_to "編集", edit_failure_path(failure), class: "card-link" %>
              <%= link_to "削除", failure_path(failure), data: { turbo_method: :delete, turbo_confirm: "本当に削除しますか？" }, class: "card-link text-danger" %>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
<% else %>
  <div class="alert alert-info">
    まだ失敗記録がありません。「新しい失敗を記録」ボタンから記録を始めましょう。
  </div>
<% end %> 